# Workflows

`checks.yml` provides the initial public repository status check named exactly `checks`.

The first seed validates documents, task graph, public-data boundary, Ruleset recipe and text integrity. `HW-00` must extend this same job to include frozen pnpm install, build, lint, typecheck, test and governance without renaming the required check context.
